% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_serodynamics_stan_2a.R
\name{run_serodynamics_stan_2a}
\alias{run_serodynamics_stan_2a}
\title{Fit the Chapter 2 serodynamics model (Model 2a) with Stan}
\usage{
run_serodynamics_stan_2a(
  data,
  file_mod = NULL,
  nchain = 4,
  nadapt = 1000,
  niter = 1000,
  adapt_delta = 0.95,
  max_treedepth = 12,
  init = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{a \code{case_data} \link{data.frame}.}

\item{file_mod}{optional explicit path to a \code{.stan} file. Defaults to the
bundled \code{model_2a.stan}. (Validation code can point this at an alternative
model with the same data/prior interface -- e.g. a block-diagonal Chapter 1
baseline -- without this function depending on that file.)}

\item{nchain}{number of chains.}

\item{nadapt}{warmup iterations (\code{iter_warmup}).}

\item{niter}{sampling iterations (\code{iter_sampling}).}

\item{adapt_delta}{target acceptance (default 0.95).}

\item{max_treedepth}{maximum tree depth (default 12 -- see description).}

\item{init}{optional CmdStanR \code{init}. Leave \code{NULL} (default) unless testing
shows otherwise.}

\item{seed}{optional RNG seed.}

\item{...}{additional arguments passed to
\code{\link[=prep_priors_serodynamics_stan_2a]{prep_priors_serodynamics_stan_2a()}} (e.g. \code{mu_hyp_param}).}
}
\value{
the CmdStanR fit object (call \verb{$summary()}, \verb{$draws()} on it). Key
quantities: \code{mu_par} (population means), \code{cross_cor} (same-parameter
cross-biomarker correlations), \code{Sigma_joint} (the assembled 10x10 covariance,
for conditional prediction), and \code{log_lik} (for LOO/WAIC). The antigens are
attached as attribute \code{"antigens"}.
}
\description{
Fits the within-host antibody model with the Chapter 2 Model 2a
("Chapter1+alpha") covariance: Chapter 1's two free 5x5 within-biomarker
blocks are kept unchanged and only the 5 same-parameter cross-biomarker
covariances are added (a diagonal cross-block), so the model strictly nests
Chapter 1 (35 vs 30 covariance parameters). The Stan model fitted is
\code{inst/extdata/model_2a.stan}.

This runner is \strong{self-contained on \code{main}}: it does not call any function
added by the (unmerged) Chapter 1 Stan PR. It uses
\code{\link[=prep_data_serodynamics_stan_2a]{prep_data_serodynamics_stan_2a()}} and \code{\link[=prep_priors_serodynamics_stan_2a]{prep_priors_serodynamics_stan_2a()}}.

Default sampler settings copy the values validated for the Chapter 1 Stan
model on this likelihood. In particular \code{max_treedepth = 12} and the default
initialization are deliberate: raising tree depth or narrowing \code{init} let
chains escape into a degenerate corner of the decay ridge in Chapter 1. Model
2a adds only 5 cross-coupling parameters, so it should behave similarly;
still, raise \code{nadapt}/\code{niter} above the small defaults for production runs.
}
\author{
Kwan Ho Lee
}
