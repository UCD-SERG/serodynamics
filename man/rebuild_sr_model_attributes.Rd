% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rebuild_sr_model_attributes.R
\name{rebuild_sr_model_attributes}
\alias{rebuild_sr_model_attributes}
\title{Rebuild \code{sr_model} output attributes in a stable order}
\usage{
rebuild_sr_model_attributes(x, mod_atts)
}
\arguments{
\item{x}{A \link{data.frame} / \link[tibble:tbl_df]{tibble::tbl_df} of combined MCMC output.}

\item{mod_atts}{A named \link{list} of \code{ggmcmc} metadata attributes, containing
\code{nChains}, \code{nParameters}, \code{nIterations}, \code{nBurnin}, and \code{nThin}.}
}
\value{
\code{x} with its attributes rebuilt in a stable order and the
\code{sr_model} class prepended.
}
\description{
\code{rebuild_sr_model_attributes()} reconstructs the attribute list of the
combined \code{\link[=run_mod]{run_mod()}} output in a fixed order, ensuring \code{class} appears
immediately after \code{names} and \code{row.names}.
The \code{dplyr} operations used to assemble the output can carry \code{ggmcmc}
attributes (\code{nChains}, etc.) into the result and push \code{class} to the end,
so this helper rebuilds the attributes explicitly. \code{mod_atts} (a named
selection from the \code{ggmcmc::ggs()} object) is the authoritative source for
the \code{ggmcmc}-style metadata attributes.
}
\author{
Sam Schildhauer
}
\keyword{internal}
