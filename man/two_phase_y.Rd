% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_phase_y.R
\name{two_phase_y}
\alias{two_phase_y}
\title{Two-phase Antibody Kinetics (deterministic curve)}
\usage{
two_phase_y(t, y0, y1, t1, alpha, rho)
}
\arguments{
\item{t}{Numeric vector of times (days).}

\item{y0}{Baseline level (> 0).}

\item{y1}{Peak level (> y0).}

\item{t1}{Time of peak (> 0, in days).}

\item{alpha}{Positive decay-rate parameter.}

\item{rho}{Shape parameter (> 1).}
}
\value{
A numeric vector of length \code{length(t)} with expected antibody levels.
}
\description{
\code{two_phase_y()} returns the expected antibody level at time \code{t} for a
simple two-phase model:
an exponential rise from \code{y0} to \code{y1} up to time \code{t1}, followed by a
power-law decay controlled by \code{alpha} and \code{rho}.

This is used for simulation and simple trajectory checks.
}
\details{
The function is fully vectorized in \code{t}. For numerical stability during
decay, the inner term is clamped to be at least \code{1e-12}.
}
\examples{
# Basic usage
t <- c(0, 7, 14, 30, 90)
two_phase_y(t, y0 = 1, y1 = 6, t1 = 14, alpha = 0.02, rho = 2)

# A quick plot (if graphics is available)
if (interactive()) {
  tt <- seq(0, 180, by = 1)
  yy <- two_phase_y(tt, y0 = 1, y1 = 8, t1 = 21, alpha = 0.015, rho = 1.8)
  plot(tt, yy, type = "l", xlab = "Days", ylab = "Antibody level",
       main = "Two-phase kinetics: rise then decay")
}
}
\seealso{
\code{\link[=simulate_multi_b_long]{simulate_multi_b_long()}}, \code{\link[=simulate_multi_b_long2]{simulate_multi_b_long2()}},
\code{\link[=make_ab_vec]{make_ab_vec()}}
}
\author{
Kwan Ho Lee
}
