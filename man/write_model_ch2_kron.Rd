% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_model_ch2_kron.R
\name{write_model_ch2_kron}
\alias{write_model_ch2_kron}
\title{Write the Chapter 2 Kronecker JAGS model}
\usage{
write_model_ch2_kron(path = "model_ch2_kron.jags")
}
\arguments{
\item{path}{File path to write (default \code{"model_ch2_kron.jags"}).}
}
\value{
Invisibly returns \code{path}.
}
\description{
\code{write_model_ch2_kron()} emits a JAGS model file that places a
Kronecker precision \eqn{T = T_B \otimes T_P} over stacked
per-biomarker parameters (5 per biomarker).

Expected data in the JAGS environment includes:
\itemize{
\item scalar: \code{n_blocks}, \code{nsubj}, \code{n_params} (should be 5)
\item hypermeans: \code{mu.hyp[b, ]}, hyper-precisions: \code{prec.hyp[b, , ]}
\item Wishart pieces: \code{OmegaP[5,5]}, \code{nuP}, \code{OmegaB[n_blocks,n_blocks]},
\code{nuB}
\item measurement: \code{smpl.t}, \code{nsmpl}, \code{prec.logy.hyp}
}
}
\examples{
# Write the model to a temp file and confirm it exists
p <- write_model_ch2_kron(file.path(tempdir(), "model_ch2_kron.jags"))
print(file.exists(p))
}
\author{
Kwan Ho Lee
}
