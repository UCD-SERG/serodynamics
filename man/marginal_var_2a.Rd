% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal_var_2a.R
\name{marginal_var_2a}
\alias{marginal_var_2a}
\title{Marginal within-biomarker variance under the factor model}
\usage{
marginal_var_2a(prec_par_k, lambda_k)
}
\arguments{
\item{prec_par_k}{A \verb{P x P} precision \link{matrix} for biomarker \code{k} (the
Wishart node \code{prec.par[k,,]}).}

\item{lambda_k}{A length-\code{P} \link{numeric} \link{vector} of loadings for biomarker
\code{k}.}
}
\value{
A length-\code{P} \link{numeric} \link{vector} of marginal variances.
}
\description{
The Model 2a factor construction is
\code{par[i,k,p] = mu[k,p] + w[i,k,p] + lambda[k,p] * f[i,p]}, so the marginal
variance of parameter \code{p} for biomarker \code{k} is the within-biomarker variance
of \code{w} plus the squared loading:
\deqn{\mathrm{Var}(par_{k,p}) = (\mathrm{prec.par}_k^{-1})_{pp} +
\lambda_{k,p}^2.}
This pure helper returns that marginal variance vector for one biomarker, for
a single MCMC draw.
}
