% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_priors_2a.R
\name{prep_priors_2a}
\alias{prep_priors_2a}
\title{Prepare priors for Model 2a}
\usage{
prep_priors_2a(max_antigens, prec_lambda = 0.25, ...)
}
\arguments{
\item{max_antigens}{An \link{integer}: number of biomarkers (must be >= 2 for
Model 2a, which couples biomarkers).}

\item{prec_lambda}{Prior precision of the factor loadings (see
\code{\link[=add_factor_priors]{add_factor_priors()}}). Default \code{0.25}.}

\item{...}{Additional Chapter 1 prior arguments forwarded to \code{\link[=prep_priors]{prep_priors()}}
(e.g. \code{mu_hyp_param}, \code{omega_param}, \code{wishdf_param}).}
}
\value{
A prior \link{list} suitable for \code{model_2a.jags}.
}
\description{
Thin wrapper that builds the full Model 2a prior list: it calls
\code{\link[=prep_priors]{prep_priors()}} for the Chapter 1 hyperpriors (\code{mu.hyp}, \code{prec.hyp},
\code{omega}, \code{wishdf}, \code{prec.logy.hyp}, \code{n_params}) and then appends the
factor-loading prior via \code{\link[=add_factor_priors]{add_factor_priors()}}.

Because Model 2a keeps the Chapter 1 priors intact, all Chapter 1 prior
arguments are forwarded unchanged through \code{...}.
}
