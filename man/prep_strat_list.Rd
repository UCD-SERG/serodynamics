% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_strat_list.R
\name{prep_strat_list}
\alias{prep_strat_list}
\title{Prepare and validate a stratification list}
\usage{
prep_strat_list(data, strat)
}
\arguments{
\item{data}{A \code{\link[base:data.frame]{base::data.frame()}} containing the stratification column.}

\item{strat}{A \link{character} string naming the stratification column, or \code{NA}
to run the model without stratification.}
}
\value{
A vector of stratum labels to loop over.
}
\description{
\code{prep_strat_list()} builds the vector of stratum labels that
\code{\link[=run_serodynamics]{run_serodynamics()}} iterates over.
When \code{strat} is \code{NA}, a single pseudo-stratum (\code{"None"}) is returned so the
model runs once on the full data set.
Otherwise the unique values of \code{data[[strat]]} are returned, with factor
columns coerced to their character labels so the loop iterates over labels
rather than the underlying integer codes.
Rows with a missing stratification value are dropped with a warning, since
they cannot be assigned to a stratum.
}
\author{
Sam Schildhauer
}
\keyword{internal}
