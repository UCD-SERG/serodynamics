% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_cross_2a.R
\name{summarize_cross_2a}
\alias{summarize_cross_2a}
\title{Summarize cross-biomarker covariance from a Model 2a fit}
\usage{
summarize_cross_2a(
  mcmc,
  antigens = NULL,
  param_names = NULL,
  probs = c(0.025, 0.5, 0.975)
)
}
\arguments{
\item{mcmc}{A \link[coda:mcmc.list]{coda::mcmc.list} from \code{\link[=run_mod_2a]{run_mod_2a()}} (must contain monitored
\code{lambda} and \code{prec.par} nodes).}

\item{antigens}{Optional length-2 \link{character} vector of biomarker labels,
used only for the printed pair label.}

\item{param_names}{Optional length-\code{P} \link{character} vector of kinetic
parameter names. Defaults to the log-scale names.}

\item{probs}{Quantiles for the credible interval. Default
\code{c(0.025, 0.5, 0.975)}.}
}
\value{
A \link{data.frame} with one row per kinetic parameter and columns
\code{param}, \code{pair}, \code{cov_med}, \code{cov_lo}, \code{cov_hi}, \code{cor_med}, \code{cor_lo},
\code{cor_hi}.
}
\description{
Reads posterior draws of the loadings (\code{lambda}) and within-biomarker
precisions (\code{prec.par}) from a Model 2a \code{mcmc.list} and returns, per kinetic
parameter, the posterior median and 95\% credible interval of:
\itemize{
\item \code{c_p}   = same-parameter cross-biomarker covariance
(\eqn{\lambda_{1,p}\lambda_{2,p}});
\item \code{rho_p} = the corresponding cross-biomarker correlation.
}

The per-draw algebra is delegated to the small pure helpers
\code{\link[=cross_cov_from_loadings]{cross_cov_from_loadings()}} and \code{\link[=cross_cor_from_draw_2a]{cross_cor_from_draw_2a()}}, so this function
only handles extraction and summarization.
}
