% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_priors.R
\name{clean_priors}
\alias{clean_priors}
\title{Drop legacy/unused prior fields}
\usage{
clean_priors(x)
}
\arguments{
\item{x}{A \code{\link[base:list]{base::list()}} of priors (e.g., from \code{\link[=prep_priors]{prep_priors()}}).}
}
\value{
A \code{\link[base:list]{base::list()}} with the legacy fields removed.
}
\description{
\code{clean_priors()} removes elements from a priors list that are not
used by the Kronecker model. This helps avoid passing unused or
conflicting parameters (like \code{omega}, \code{wishdf}, or \code{prec.par}) into JAGS.
}
\examples{
# Example: remove unused fields
priors <- list(
  omega = 1,
  wishdf = 10,
  mu.hyp = matrix(0, 1, 1),
  prec.par = 5
)

cleaned <- clean_priors(priors)
print(names(cleaned))
}
\author{
Kwan Ho Lee
}
