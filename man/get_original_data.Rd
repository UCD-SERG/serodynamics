% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_residuals_helpers.R
\name{get_original_data}
\alias{get_original_data}
\title{Uses original data from correct source for \code{plot_residuals()}}
\usage{
get_original_data(
  model,
  original_data = NULL,
  strat = attr(model, "strat"),
  color_strat = FALSE,
  color_by_strat = NULL,
  facet_strat = FALSE,
  facet_by_strat = NULL
)
}
\arguments{
\item{model}{An \code{sr_model} object (returned by \code{\link[=run_serodynamics]{run_serodynamics()}}), with
\code{original_data}, \code{strat}, and \code{decay_type} attributes (see
\code{\link[=calc_fit_mod]{calc_fit_mod()}}).}

\item{original_data}{\link{data.frame}; the original dataset fed into
\code{run_serodynamics()}. Must be included if \code{facet_by_strat} or
\code{color_by_strat} include a variable that was not specified as \code{strat} in
\code{run_serodynamics()}.}

\item{strat}{The stratification specified in the original
\code{run_serodynamics()} model.}

\item{color_strat}{The color stratification specified in \code{facet_by_strat}.}

\item{color_by_strat}{\link{character}; colors residual plot by the specified
stratification variable. MAE is not calculated by this variable. Must
include the original data set if not stratifying by \code{strat} variable
specified in \code{run_serodynamics()}. Default \code{NULL}.}

\item{facet_strat}{The facet stratification specified in \code{facet_by_strat}.}

\item{facet_by_strat}{\link{character}; facets residual plot and
calculates MAE by specified stratification variable. Must include the
original data set if not stratifying by \code{strat} variable specified in
\code{run_serodynamics()}. Default \code{NULL}.}
}
\description{
\code{get_original_data} will be used when a \code{facet_by_strat} or \code{color_by_strat}
is called in order to access the stratifying variable. When the stratified
variable occurs in the original \code{run_serodynamics()} call, the
\code{original_data} attached to the output will be used. If the stratification
was not used in the \code{run_serodynamics()} call, then the \code{original_data} will
need to be attached as an option.
}
\keyword{internal}
