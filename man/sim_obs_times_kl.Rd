% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_obs_time_kl.R
\name{sim_obs_times_kl}
\alias{sim_obs_times_kl}
\title{Simulate observation (visit) times for one participant}
\usage{
sim_obs_times_kl(
  followup_interval,
  followup_variance,
  n_obs,
  spacing = c("uniform", "log"),
  max_n_obs = NULL,
  t_min = 3,
  t_max = 400
)
}
\arguments{
\item{followup_interval}{\link{integer} mean days between visits (used when
\code{spacing = "uniform"})}

\item{followup_variance}{\link{integer} jitter (+/- days) between visits (used
when \code{spacing = "uniform"})}

\item{n_obs}{\link{integer} number of visits for this participant}

\item{spacing}{\link{character} visit-time scheme: \code{"uniform"} (default) or
\code{"log"}}

\item{max_n_obs}{\link{integer} length of the full visit grid (\strong{required} when
\code{spacing = "log"}; typically the same \code{max_n_obs} passed to
\link{sim_case_data})}

\item{t_min}{\link{numeric} first non-zero visit day for the log grid
(default 3; used when \code{spacing = "log"})}

\item{t_max}{\link{numeric} last visit day for the log grid, reached by a
participant with \code{max_n_obs} visits (default 400; used when
\code{spacing = "log"})}
}
\value{
a numeric \link{vector} of visit days (length \code{n_obs}), starting at 0
}
\description{
Generates the day-offsets of a participant's visits, starting at day 0
(infection / baseline). Two schemes are available:
}
\details{
\itemize{
\item \code{spacing = "uniform"} (default, \strong{original behaviour}): visits are equally
spaced by \code{followup_interval} days, with small random jitter of
+/- \code{followup_variance} days between visits.
\item \code{spacing = "log"} (\strong{new, optional}): visits follow a fixed log-spaced
grid -- dense early, sparse late -- so the antibody \strong{peak} (a few days
after infection) and the slow \strong{decay tail} (months later) are both
captured. A participant with \code{n_obs} visits receives the first \code{n_obs}
points of the grid \verb{c(0, exp-spaced(t_min .. t_max, max_n_obs - 1))}.
}

The \code{"uniform"} path is byte-for-byte the original implementation, so
existing calls (three positional arguments) are unaffected.
}
\examples{
set.seed(1)
# original uniform scheme (unchanged):
sim_obs_times(followup_interval = 7, followup_variance = 1, n_obs = 5)

# new log-spaced scheme (dense early, sparse late):
sim_obs_times(n_obs = 15, spacing = "log", max_n_obs = 15,
              t_min = 3, t_max = 400)
}
