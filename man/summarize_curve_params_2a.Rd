% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_curve_params_2a.R
\name{summarize_curve_params_2a}
\alias{summarize_curve_params_2a}
\title{Summarize shared curve-parameter posteriors}
\usage{
summarize_curve_params_2a(mcmc, with_loadings = FALSE, param_names = NULL)
}
\arguments{
\item{mcmc}{A \link[coda:mcmc.list]{coda::mcmc.list} (or a named draws \link{matrix}) containing
monitored \code{mu.par} and \code{prec.par} (and \code{lambda} when \code{with_loadings}).}

\item{with_loadings}{\link{logical}; add the squared factor loadings to the
within-biomarker variance (TRUE for Model 2a, FALSE for Chapter 1).}

\item{param_names}{Optional length-\code{P} parameter labels (defaults to the
log-scale names).}
}
\value{
A \link{data.frame} with columns \code{biomarker} (index), \code{param},
\code{mean_med} (median of \code{mu.par}), and \code{var_med} (median within-biomarker
variance).
}
\description{
Extracts the quantities that Chapter 1 (\code{model.jags}) and Model 2a
(\code{model_2a.jags}) have in \strong{common} so they can be compared directly:
the population means \code{mu.par[k, p]} and the within-biomarker variances.
For Model 2a the within-biomarker variance is the \emph{marginal} variance
\code{diag(solve(prec.par[k])) + lambda[k, ]^2} (set \code{with_loadings = TRUE});
for Chapter 1 there are no loadings, so it is just \code{diag(solve(prec.par[k]))}
(\code{with_loadings = FALSE}). Posterior medians are returned.

Pure extraction/summary (delegates the variance algebra to
\code{\link[=marginal_var_2a]{marginal_var_2a()}}); no fitting.
}
