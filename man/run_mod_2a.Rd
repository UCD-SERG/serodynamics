% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mod_2a.R
\name{run_mod_2a}
\alias{run_mod_2a}
\title{Fit Model 2a (Chapter 1 + alpha) with JAGS}
\usage{
run_mod_2a(
  data,
  file_mod = serodynamics_example("model_2a.jags"),
  nchain = 4,
  nadapt = 1000,
  nburn = 1000,
  nmc = 1000,
  niter = 4000,
  prec_lambda = 0.25,
  extra_monitors = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{serocalculator} case-data \link{data.frame} with \strong{two}
biomarkers (e.g. \code{HlyE_IgG} / \code{HlyE_IgA}).}

\item{file_mod}{Path to the Model 2a JAGS file. Defaults to the packaged
\code{model_2a.jags}.}

\item{nchain, nadapt, nburn, nmc, niter}{Standard MCMC controls, matching
\code{\link[=run_mod]{run_mod()}} (chains, adaptation, burn-in, samples kept, total iterations).}

\item{prec_lambda}{Prior precision of the factor loadings. Default \code{0.25}.}

\item{extra_monitors}{Optional \link{character} vector of additional nodes to
monitor (e.g. \code{"y0"}). The covariance machinery always monitors \code{lambda},
\code{mu.par}, \code{prec.par}, and \code{prec.logy}.}

\item{...}{Additional Chapter 1 prior arguments forwarded to
\code{\link[=prep_priors_2a]{prep_priors_2a()}}.}
}
\value{
A list of class \code{"model_2a_fit"} with elements:
\itemize{
\item \code{mcmc}: the \link[coda:mcmc.list]{coda::mcmc.list} of posterior draws;
\item \code{cross}: a tidy \link{data.frame} of posterior cross-biomarker covariance
\code{c_p} and correlation \code{rho_p} per kinetic parameter;
\item \code{antigens}: the two biomarker labels;
\item \code{prec_lambda}: the loading-prior precision used;
\item \code{runjags}: the raw \code{runjags} object (for diagnostics such as PSRF).
}
}
\description{
Fits the Model 2a extension of \code{model.jags}. Model 2a keeps Chapter 1's
within-biomarker covariance blocks intact (\code{Sigma_G}, \code{Sigma_A} via the
same Wishart prior) and adds same-parameter cross-biomarker covariances
through a shared latent factor per kinetic parameter:
\deqn{par_{i,k,p} = \mu_{k,p} + w_{i,k,p} + \lambda_{k,p}\, f_{i,p},}
so that for two biomarkers the cross-biomarker covariance is
\eqn{c_p = \lambda_{1,p}\lambda_{2,p}} and Chapter 1 is recovered exactly
when all \code{lambda = 0}.

This wrapper is intentionally lean and decomposed: it builds the JAGS input
(\code{\link[=jags_data_2a]{jags_data_2a()}}), runs the sampler, and returns the raw MCMC plus a tidy
cross-biomarker summary (\code{\link[=summarize_cross_2a]{summarize_cross_2a()}}). It does \strong{not} reproduce
the full \code{sr_model} post-processing of \code{\link[=run_mod]{run_mod()}}; the goal is a small,
debuggable object focused on the Chapter 2 covariance question.
}
\examples{
# Model 2a (Chapter 1 + alpha) -- example usage.
# Mirrors run_mod-examples.R: one representative fit when JAGS is available.
# Heavier validation / comparison calls are shown commented out (run locally).
if (!is.element(runjags::findjags(), c("", NULL))) {
  library(serodynamics)

  # nepal_sees ships as `case_data` with exactly two biomarkers:
  # HlyE_IgG and HlyE_IgA. It is already in the right format -- pass it directly.
  data(nepal_sees)

  fit <- run_mod_2a(
    data = nepal_sees,
    file_mod = serodynamics_example("model_2a.jags"),
    nchain = 4, nadapt = 100, nburn = 100, nmc = 1000, niter = 2000
  )

  # cross-biomarker (IgG ~ IgA) covariance & correlation, per kinetic parameter
  print(fit$cross)

  # ---- run these locally (Mercury) at full length; omitted from routine check:
  # validate_recovery_2a()                  # recover a known correlation
  # validate_nesting_2a()                   # ~0 when there is none
  # cmp <- compare_mod_2a(nepal_sees)       # Chapter 1 vs Model 2a
  # print(cmp$shared); print(cmp$added)     # shared params + the addition
}
}
\author{
Kwan Ho Lee
}
