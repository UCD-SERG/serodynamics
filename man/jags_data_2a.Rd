% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jags_data_2a.R
\name{jags_data_2a}
\alias{jags_data_2a}
\title{Build the combined JAGS input list for Model 2a}
\usage{
jags_data_2a(data, prec_lambda = 0.25, add_newperson = TRUE, ...)
}
\arguments{
\item{data}{A \link{data.frame} in \code{serocalculator} case-data format (columns
\code{antigen_iso}, \code{visit_num}, a value column, and a time column).}

\item{prec_lambda}{Prior precision of the factor loadings, forwarded to
\code{\link[=prep_priors_2a]{prep_priors_2a()}}. Default \code{0.25}.}

\item{add_newperson}{\link{logical}; forwarded to \code{\link[=prep_data]{prep_data()}}. Default \code{TRUE}
to match Chapter 1 (adds a dummy subject for posterior prediction).}

\item{...}{Additional Chapter 1 prior arguments forwarded to
\code{\link[=prep_priors_2a]{prep_priors_2a()}}.}
}
\value{
A named \link{list} of JAGS inputs, with attribute \code{"antigens"} (the
biomarker labels) and \code{"ids"} (subject ids).
}
\description{
Assembles the single named list that \code{model_2a.jags} consumes, by combining
the prepared longitudinal data (\code{\link[=prep_data]{prep_data()}}, reused unchanged from
Chapter 1) with the Model 2a priors (\code{\link[=prep_priors_2a]{prep_priors_2a()}}). The biomarker
labels from \code{prep_data()} are carried along as an attribute so downstream
summaries can label the two biomarkers.
}
