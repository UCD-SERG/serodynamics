% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initsfunction.R
\name{build_chain_inits}
\alias{build_chain_inits}
\title{Build stable JAGS chain initial values}
\usage{
build_chain_inits(longdata, chain, n_params)
}
\arguments{
\item{longdata}{A \code{prepped_jags_data} \link{list} as returned by \code{\link[=prep_data]{prep_data()}}.
It must include \code{nsubj} and \code{n_antigen_isos}.}

\item{chain}{An \link{integer} chain index between 1 and 4.}

\item{n_params}{An \link{integer} giving the number of subject-level parameters
in the selected JAGS model. Supported values are 4 (exponential decay) and
5 (power decay).

The returned \code{par} array follows the JAGS model layout:
\code{par[, , 1] = log(y0)},
\code{par[, , 2] = log(y1 - y0)},
\code{par[, , 3] = log(t1)},
\code{par[, , 4] = log(alpha)},
and, for the power-decay model only,
\code{par[, , 5] = log(shape - 1)}.
Slices 4 and 5 are initialized to fixed log-scale values so that the
piecewise recovery expression stays numerically valid during JAGS'
initial-value checks across platforms.}
}
\value{
A \link{list} suitable for the \code{inits} argument of
\code{\link[runjags:run.jags]{runjags::run.jags()}}, containing \code{.RNG.seed}, \code{.RNG.name}, and a \code{par}
array with dimensions \verb{nsubj x n_antigen_isos x n_params}.
}
\description{
Build stable JAGS chain initial values
}
\keyword{internal}
