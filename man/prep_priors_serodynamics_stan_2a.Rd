% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_priors_serodynamics_stan_2a.R
\name{prep_priors_serodynamics_stan_2a}
\alias{prep_priors_serodynamics_stan_2a}
\title{Prepare priors for the Chapter 2 Stan models}
\usage{
prep_priors_serodynamics_stan_2a(
  max_antigens,
  mu_hyp_param = c(1, 7, 1, -4, -1),
  prec_hyp_param = c(1, 1/9, 1, 1/9, 1),
  omega_param = c(1, 50, 1, 10, 1),
  wishdf_param = 20,
  prec_logy_hyp_param = c(4, 1)
)
}
\arguments{
\item{max_antigens}{number of antigen-isotype combinations.}

\item{mu_hyp_param}{length-5 population-mean prior means
\verb{(y0, y1, t1, alpha, shape)} on the log scale.}

\item{prec_hyp_param}{length-5 prior precisions (diagonal) for the population
mean (weakly informative, SD ~ 3, for HMC stability).}

\item{omega_param}{length-5 Wishart scale diagonal.}

\item{wishdf_param}{Wishart degrees of freedom (scalar).}

\item{prec_logy_hyp_param}{length-2 gamma(shape, rate) for measurement
precision.}
}
\value{
a named \link{list} with \code{n_params}, \code{mu_hyp}, \code{prec_hyp}, \code{omega},
\code{wishdf}, \code{prec_logy_hyp}, sized for \code{max_antigens}.
}
\description{
Builds the per-antigen prior inputs (\code{mu_hyp}, \code{prec_hyp}, \code{omega}, \code{wishdf},
\code{prec_logy_hyp}) consumed by \code{model_2a.stan} and \code{model_2a_indep.stan}.

These are the \strong{same} per-antigen prior inputs as the Chapter 1 Stan model
(\code{prep_priors_stan()}), reproduced here so the Chapter 2 branch does not
depend on the (unmerged) Chapter 1 Stan PR. Keeping the inputs identical also
makes the Model 2a vs. Chapter 1 comparison fair: the marginal scale prior of
each parameter, and the within-antigen correlation prior, match Chapter 1.
\code{model_2a.stan} combines these per-antigen inputs into a single joint
covariance prior (see that file's \verb{transformed data} block); \code{model_2a_indep}
uses them block-diagonally exactly as Chapter 1 does.
}
\author{
Kwan Ho Lee
}
