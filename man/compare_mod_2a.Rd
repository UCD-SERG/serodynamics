% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_mod_2a.R
\name{compare_mod_2a}
\alias{compare_mod_2a}
\title{Compare Chapter 1 and Model 2a on the same data}
\usage{
compare_mod_2a(
  data,
  nchain = 4,
  nadapt = 1000,
  nburn = 1000,
  nmc = 1000,
  niter = 4000,
  prec_lambda = 0.25,
  dic = FALSE,
  ...
)
}
\arguments{
\item{data}{A two-biomarker \code{serocalculator} case-data \link{data.frame}
(e.g. \code{nepal_sees}).}

\item{nchain, nadapt, nburn, nmc, niter}{MCMC controls applied to \strong{both} fits.}

\item{prec_lambda}{Factor-loading prior precision (Model 2a only).}

\item{dic}{\link{logical}; attempt to extract DIC from each \code{runjags} fit
(best-effort; may re-run the models and can be slow). Default \code{FALSE}.}

\item{...}{Prior arguments forwarded to \strong{both} \code{\link[=prep_priors]{prep_priors()}} and
\code{\link[=prep_priors_2a]{prep_priors_2a()}}.}
}
\value{
A list of class \code{"model_2a_comparison"} with:
\itemize{
\item \code{shared}: \link{data.frame} comparing \code{mean}/\code{var} per biomarker x parameter
(\verb{*_ch1}, \verb{*_2a}, and \verb{*_absdiff});
\item \code{cross}: Model 2a's cross-biomarker covariance/correlation summary;
\item \code{max_mean_absdiff}, \code{max_var_absdiff}: worst-case shared-parameter
discrepancies (small = consistent);
\item \code{added}: the parameters whose \code{c_p} credible interval excludes zero;
\item \code{dic_ch1}, \code{dic_2a}: raw DIC objects when \code{dic = TRUE} (else \code{NULL});
\item \code{fits}: the two underlying fit objects.
}
}
\description{
Fits \strong{both} the Chapter 1 model (\code{\link[=fit_chapter1_lean]{fit_chapter1_lean()}}, the same posterior
as \code{\link[=run_mod]{run_mod()}}) and \strong{Model 2a} (\code{\link[=run_mod_2a]{run_mod_2a()}}) to the same data, then
reports:
\enumerate{
\item \strong{What stays the same} — the shared population means \code{mu.par} and the
within-biomarker variances, side by side, with absolute differences.
Because Model 2a strictly nests Chapter 1, these should agree within
MCMC error; large differences would signal a problem.
\item \strong{What Model 2a adds} — the cross-biomarker covariance \code{c_p} /
correlation \code{rho_p}, which Chapter 1 cannot represent (it is
structurally zero there).
}

Use this to answer "what changed when I added cross-biomarker covariance?".

\strong{On "what improved".} A point-estimate comparison shows \emph{consistency plus
the new term}; it does not by itself establish that Model 2a is \emph{better}.
A rigorous improvement claim needs a model-selection criterion (DIC/WAIC; set
\code{dic = TRUE} for a best-effort DIC from each \code{runjags} fit) and, ultimately,
the downstream predictive task (e.g. time-since-infection / seroincidence
accuracy — MAE, RMSE, CrI coverage), which is the Chapter 2 simulation study
rather than a single function.
}
