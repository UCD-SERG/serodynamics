% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_jags_densitydx.R,
%   R/plot_jags_densitydx_dev.R
\name{plot_jags_dens}
\alias{plot_jags_dens}
\title{Density Plot Diagnostics}
\usage{
plot_jags_dens(
  data,
  iso = unique(visualize_jags_sub$Iso_type),
  param = unique(visualize_jags_sub$Parameter_sub),
  strat = NA
)

plot_jags_dens(
  data,
  iso = unique(visualize_jags_sub$Iso_type),
  param = unique(visualize_jags_sub$Parameter_sub),
  strat = NA
)
}
\arguments{
\item{data}{A \code{\link[base:list]{base::list()}} outputted from run_mod().}

\item{iso}{Specify \link{string} to produce plots of only a specific
antigen/antibody combination, entered with quotes. Default outputs all
antigen/antibody combinations.}

\item{param}{Specify \link{string} to produce plots of only a specific parameter,
entered with quotes. Options include:
\itemize{
\item \code{alpha} = posterior estimate of decay rate
\item \code{r} = posterior estimate of shape parameter
\item \code{t1} = posterior estimate of time to peak
\item \code{y0} = posterior estimate of baseline antibody concentration
\item \code{y1} = posterior estimate of peak antibody concentration
}}

\item{strat}{Specify \link{string} to produce plots of specific stratification
entered in quotes.}

\item{name}{description}
}
\value{
A \link{list} of [] objects producing density plots for all the specified
input.

A \link{list} of [] objects producing density plots for all the specified
input.
}
\description{
plot_jags_dens() takes a \link{list} output from the \code{\link[=run_mod]{run_mod()}}
function to create density plots for each chain run in the mcmc estimation.
Defaults will produce every combination of antigen/antibody, parameters,
and stratifications, unless otherwise specified.
Antigen/antibody combinations and stratifications will vary by analysis.
The antibody dynamic curve includes the following parameters:
\itemize{
\item y0 = baseline antibody concentration
\item y1 = peak antibody concentration
\item t1 = time to peak
\item r = shape parameter
\item alpha = decay rate
}

plot_jags_dens() takes a \link{list} output from the \code{\link[=run_mod]{run_mod()}}
function to create density plots for each chain run in the mcmc estimation.
Defaults will produce every combination of antigen/antibody, parameters,
and stratifications, unless otherwise specified.
Antigen/antibody combinations and stratifications will vary by analysis.
The antibody dynamic curve includes the following parameters:
\itemize{
\item y0 = baseline antibody concentration
\item y1 = peak antibody concentration
\item t1 = time to peak
\item r = shape parameter
\item alpha = decay rate
}
}
\examples{
plot_jags_dens(
    data = [list], #A [serodynamics::run_mod()] [list] output
    iso = "hlya_IgG", #A [string] specifying antigen/antibody of interest.
    Default are all antigen/antibody combinations.
    param = "alpha",  #A [string] specifying parameter of interest. Default
    is all parameters.
    strat = 4),  #A [string] specifying stratification of interest. Default
    is all stratifications.
plot_jags_dens(
    data = [list], #A [serodynamics::run_mod()] [list] output
    iso = "hlya_IgG", #A [string] specifying antigen/antibody of interest. 
    Default are all antigen/antibody combinations. 
    param = "alpha",  #A [string] specifying parameter of interest. Default
    is all parameters. 
    strat = 4),  #A [string] specifying stratification of interest. Default
    is all stratifications.
}
\author{
Sam Schildhauer
}
