% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_params_2a.R
\name{sim_params_2a}
\alias{sim_params_2a}
\title{Simulate subject-level parameters with a known Model 2a covariance}
\usage{
sim_params_2a(n, mu_g, mu_a, sigma_g, sigma_a, c_vec, seed = NULL)
}
\arguments{
\item{n}{\link{integer} number of subjects.}

\item{mu_g, mu_a}{Length-\code{P} log-scale mean vectors for biomarker 1 / 2.}

\item{sigma_g, sigma_a}{\verb{P x P} within-biomarker covariances.}

\item{c_vec}{Length-\code{P} same-parameter cross-biomarker covariances.}

\item{seed}{Optional RNG seed.}
}
\value{
A \link{list} with:
\itemize{
\item \code{log_par}: an \verb{n x (2P)} \link{matrix} of draws, columns ordered
\verb{G1..GP, A1..AP};
\item \code{sigma}: the \verb{2P x 2P} true covariance;
\item \code{rho}: the length-\code{P} true cross-biomarker correlations.
}
}
\description{
Draws \code{n} subjects' log-scale kinetic parameters for \strong{two} biomarkers from
a Model 2a multivariate normal: full within-biomarker blocks \code{Sigma_G},
\code{Sigma_A} plus a diagonal cross-biomarker block \code{C = diag(c_vec)} (assembled
by \code{\link[=build_sigma_2a]{build_sigma_2a()}}). This gives ground-truth correlated parameters for
validating \code{\link[=run_mod_2a]{run_mod_2a()}}.

The parameter order matches \code{model.jags}:
1 = log(y0), 2 = log(y1 - y0), 3 = log(t1), 4 = log(alpha),
5 = log(shape - 1). Uses a base-R Cholesky draw (no extra dependency).
}
