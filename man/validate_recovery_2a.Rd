% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_recovery_2a.R
\name{validate_recovery_2a}
\alias{validate_recovery_2a}
\title{Validate Model 2a parameter recovery}
\usage{
validate_recovery_2a(
  n = 120,
  mu_g = c(0, 3, 2.3, -4, -1),
  mu_a = c(0.2, 3.1, 2.2, -3.8, -1.1),
  sigma_g = diag(c(0.09, 0.16, 0.09, 0.16, 0.09)),
  sigma_a = diag(c(0.09, 0.16, 0.09, 0.16, 0.09)),
  c_vec = c(0.054, 0.08, 0, 0.064, 0),
  noise_sd = 0.15,
  seed = 1,
  ...
)
}
\arguments{
\item{n}{\link{integer} number of subjects. Default \code{120}.}

\item{mu_g, mu_a, sigma_g, sigma_a, c_vec}{Model 2a truth for
\code{\link[=sim_case_data_2a]{sim_case_data_2a()}}. Sensible defaults are provided.}

\item{noise_sd}{Residual SD on the log scale. Default \code{0.15}.}

\item{seed}{RNG seed for the simulation. Default \code{1}.}

\item{...}{MCMC controls forwarded to \code{\link[=run_mod_2a]{run_mod_2a()}} (e.g. \code{nchain},
\code{niter}, \code{nmc}, \code{nburn}, \code{nadapt}).}
}
\value{
A \link{data.frame} with columns \code{param}, \code{true_rho}, \code{cor_med},
\code{cor_lo}, \code{cor_hi}, and \code{verdict} ("recovered", "null ok", or "review").
}
\description{
Simulation-based check that \code{\link[=run_mod_2a]{run_mod_2a()}} recovers a \strong{known}
cross-biomarker correlation. It simulates two-biomarker longitudinal data
with a chosen \code{c_vec} (\code{\link[=sim_case_data_2a]{sim_case_data_2a()}}), fits Model 2a, and returns a
table comparing the true cross-biomarker correlation with the posterior
estimate per kinetic parameter.

Recovery is expected to be accurate for well-identified parameters (peak,
decay) and to attenuate gracefully for weakly-identified ones (baseline);
true nulls should yield credible intervals covering zero.
}
