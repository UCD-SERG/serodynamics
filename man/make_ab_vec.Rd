% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_ab_vec.R
\name{make_ab_vec}
\alias{make_ab_vec}
\title{Vectorized wrapper for serodynamics trajectory}
\usage{
make_ab_vec(ab_fun = getFromNamespace("ab", "serodynamics"))
}
\arguments{
\item{ab_fun}{A function with signature
\code{f(t, y0, y1, t1, alpha, shape)}.
Default: internal serodynamics implementation (\code{serodynamics::ab}).}
}
\value{
A vectorized function that evaluates the antibody trajectory across
multiple time points or parameter values.
}
\description{
\code{make_ab_vec()} creates a vectorized version of an antibody trajectory
function. By default, it wraps the internal
\code{serodynamics::ab()} function (fitting the two-phase model).

This wrapper allows calls with vector inputs for \verb{t, y0, y1, t1, alpha,  shape}.
}
\examples{
# Example: use default serodynamics::ab
f <- make_ab_vec()

# Evaluate at three time points
f(t = c(0, 7, 14), y0 = 1, y1 = 6, t1 = 14, alpha = 0.02, shape = 2)

# Example: override with custom toy function
toy_fun <- function(t, y0, y1, t1, alpha, shape) {
  y0 + (y1 - y0) * (1 - exp(-alpha * t))
}

f2 <- make_ab_vec(toy_fun)
f2(t = 0:5, y0 = 1, y1 = 10, t1 = 14, alpha = 0.1, shape = 2)
}
\seealso{
\code{\link[=two_phase_y]{two_phase_y()}}, \code{\link[=simulate_multi_b_long2]{simulate_multi_b_long2()}}
}
\author{
Kwan Ho Lee
}
