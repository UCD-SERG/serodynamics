% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_factor_priors.R
\name{add_factor_priors}
\alias{add_factor_priors}
\title{Append Model 2a factor priors to a Chapter 1 prior list}
\usage{
add_factor_priors(priors, prec_lambda = 0.25)
}
\arguments{
\item{priors}{A \code{curve_params_priors} list from \code{\link[=prep_priors]{prep_priors()}}.}

\item{prec_lambda}{A positive \link{numeric} scalar: prior precision of the
loadings. Default \code{0.25} (loading SD = 2), weakly informative.}
}
\value{
The input list with \code{prec.lambda} and \code{zero_p} added.
}
\description{
Model 2a reuses every Chapter 1 hyperprior unchanged and adds only two
extra inputs needed by \code{model_2a.jags}:
\itemize{
\item \code{prec.lambda}: the prior precision (1 / variance) of the factor loadings
\code{lambda[k, p]}. Smaller values are more diffuse (allow larger
cross-biomarker covariances).
\item \code{zero_p}: a length-\code{n_params} vector of zeros (the mean of the
within-biomarker random effects \code{w}).
}

Keeping this as its own one-job function makes it easy to test and to see
exactly what Model 2a adds on top of Chapter 1.
}
