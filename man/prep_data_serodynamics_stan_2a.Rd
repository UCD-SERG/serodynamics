% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_data_serodynamics_stan_2a.R
\name{prep_data_serodynamics_stan_2a}
\alias{prep_data_serodynamics_stan_2a}
\title{Prepare data for the Chapter 2 Stan models}
\usage{
prep_data_serodynamics_stan_2a(
  dataframe,
  biomarker_column = get_biomarker_names_var(dataframe),
  verbose = FALSE
)
}
\arguments{
\item{dataframe}{a \code{case_data} \link{data.frame}.}

\item{biomarker_column}{\link{character} column holding antigen-isotype names.
Defaults to the package's biomarker-name accessor.}

\item{verbose}{passed to \code{\link[=prep_data]{prep_data()}}.}
}
\value{
a named \link{list} ready for CmdStanR, with attributes \code{antigens},
\code{n_antigens}, and \code{ids} attached (used for labeling predictions).
}
\description{
Converts a \code{case_data} \link{data.frame} into the padded list \code{model_2a.stan} and
\code{model_2a_indep.stan} expect. It reuses the package's existing \code{\link[=prep_data]{prep_data()}}
(the JAGS data builder, already in \code{main}) for the ragged structure, then pads
the time and measurement arrays to a rectangular shape for Stan.

This reproduces the behavior of the Chapter 1 \code{prep_data_stan()} so the
Chapter 2 branch does not depend on the (unmerged) Chapter 1 Stan PR. The
returned list matches the \code{data} block of the Chapter 2 Stan models:
\code{nsubj}, \code{n_antigen_isos}, \code{n_params} (= 5), \code{nsmpl}, \code{max_nsmpl}, \code{smpl_t}
\verb{[nsubj, max_nsmpl]} (0-padded), and \code{logy} \verb{[nsubj, max_nsmpl, n_antigen_isos]} (0-padded). Padded positions are ignored by the models,
which loop only up to \code{nsmpl[subj]}.
}
\author{
Kwan Ho Lee
}
