% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_multi_b_long.R
\name{simulate_multi_b_long}
\alias{simulate_multi_b_long}
\title{Simulate longitudinal data (serodynamics trajectory)}
\usage{
simulate_multi_b_long(
  n_id,
  n_blocks,
  time_grid,
  sigma_p,
  sigma_b,
  mu_latent_base = c(log(1), log(5), log(30), log(0.02), log(1.5)),
  meas_sd = rep(0.22, n_blocks)
)
}
\arguments{
\item{n_id}{Integer. Number of individuals to simulate.}

\item{n_blocks}{Integer. Number of biomarkers (blocks).}

\item{time_grid}{Numeric vector of observation times (days).}

\item{sigma_p}{5×5 covariance matrix for within-biomarker parameters.}

\item{sigma_b}{\code{n_blocks}×\code{n_blocks} covariance across biomarkers.}

\item{mu_latent_base}{Numeric length-5 vector of means for the latent
parameters (on log scale) per biomarker, in the order
\code{(log y0, log(y1 - y0), log t1, log alpha, log(shape-1))}.}

\item{meas_sd}{Numeric. Measurement error SD(s) on the log scale; either a
single value recycled to all biomarkers or a length-\code{n_blocks} vector.}
}
\value{
A list with:
\itemize{
\item \code{data}: tibble with columns \code{Subject}, \code{visit_num}, \code{antigen_iso},
\code{time_days}, \code{value}.
\item \code{truth}: list with \code{m_true}, \code{sigma_p}, \code{sigma_b}, \code{sigma_total},
\code{meas_sd}, and \code{theta_latent}.
}
}
\description{
\code{simulate_multi_b_long()} simulates subject-level latent parameters with a
Kronecker covariance \eqn{\Sigma_B \otimes \Sigma_P} (5 parameters per
biomarker), then generates noisy observations on a time grid. The expected
trajectory is computed directly using \code{serodynamics::ab()}.
}
\examples{
set.seed(925)

# Define dimensions and covariances
n_blocks  <- 2
time_grid <- c(0, 7, 14, 30)

sd_p <- c(0.35, 0.45, 0.25, 0.30, 0.25)
R_p  <- matrix(0.25, 5, 5)
diag(R_p) <- 1
sigma_p <- diag(sd_p) \%*\% R_p \%*\% diag(sd_p)

R_b <- matrix(c(1, 0.5,
                0.5, 1), n_blocks, n_blocks, byrow = TRUE)
sd_b   <- rep(0.6, n_blocks)
sigma_b <- diag(sd_b) \%*\% R_b \%*\% diag(sd_b)

# Use default serodynamics trajectory 
sim <- simulate_multi_b_long(
  n_id      = 5,
  n_blocks  = n_blocks,
  time_grid = time_grid,
  sigma_p   = sigma_p,
  sigma_b   = sigma_b
)

head(sim2$data)
}
\author{
Kwan Ho Lee
}
