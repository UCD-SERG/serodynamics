% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_subject_ids.R
\name{reconcile_subject_ids}
\alias{reconcile_subject_ids}
\title{Reconcile MCMC subject indices with subject IDs}
\usage{
reconcile_subject_ids(jags_unpacked, ids)
}
\arguments{
\item{jags_unpacked}{A \link[tibble:tbl_df]{tibble::tbl_df} returned by \code{\link[=unpack_jags]{unpack_jags()}}
(after the \code{Iso_type} join), containing \code{Subject}, \code{Subnum}, \code{Param},
and \code{Parameter} columns.}

\item{ids}{A \link[tibble:tbl_df]{tibble::tbl_df} with a \code{Subject_mcmc} column (the subject ID)
and a \code{Subject} column (the MCMC index as a character string).}
}
\value{
A \link[tibble:tbl_df]{tibble::tbl_df} with a single resolved \code{Subject} column and a
\code{Parameter} column holding the cleaned parameter name.
}
\description{
\code{reconcile_subject_ids()} joins unpacked MCMC output to the subject ID
lookup table and resolves a single \code{Subject} identifier for every row.
\itemize{
\item Individual-level parameters match a row in \code{ids}, so they receive the
original subject ID.
\item Population-level parameters (e.g., \code{mu.par}, \code{prec.par}, \code{prec.logy})
have no matching ID, so they retain the parameter name produced by
\code{\link[=unpack_jags]{unpack_jags()}} as their identifier.
The temporary index columns are then dropped and the cleaned parameter
names (\code{Param}) are promoted to \code{Parameter} for downstream use.
}
}
\author{
Sam Schildhauer
}
\keyword{internal}
