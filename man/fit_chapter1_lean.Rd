% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_chapter1_lean.R
\name{fit_chapter1_lean}
\alias{fit_chapter1_lean}
\title{Lean Chapter 1 fit (for comparison with Model 2a)}
\usage{
fit_chapter1_lean(
  data,
  file_mod = serodynamics_example("model.jags"),
  nchain = 4,
  nadapt = 1000,
  nburn = 1000,
  nmc = 1000,
  niter = 4000,
  ...
)
}
\arguments{
\item{data}{A \code{serocalculator} case-data \link{data.frame}.}

\item{file_mod}{Path to the Chapter 1 JAGS model (defaults to the packaged
\code{model.jags}).}

\item{nchain, nadapt, nburn, nmc, niter}{MCMC controls (as in \code{\link[=run_mod]{run_mod()}}).}

\item{...}{Prior arguments forwarded to \code{\link[=prep_priors]{prep_priors()}}.}
}
\value{
A \code{runjags} object (its \verb{$mcmc} is a \link[coda:mcmc.list]{coda::mcmc.list}).
}
\description{
Fits the \strong{Chapter 1} model (\code{model.jags}) through the same lean path as
\code{\link[=run_mod_2a]{run_mod_2a()}} and returns the raw \code{runjags} object, monitoring the nodes
needed to compare against Model 2a (\code{mu.par}, \code{prec.par}). This is the
\strong{same model, data, priors, and posterior} as
\code{\link[=run_mod]{run_mod()}} — \code{run_mod()} simply adds post-processing on top; here we
keep the MCMC in \code{mcmc.list} form so it can be compared directly with a
Model 2a fit.

Unstratified only (matching the typhoid example workflow).
}
