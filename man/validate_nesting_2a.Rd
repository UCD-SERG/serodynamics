% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_nesting_2a.R
\name{validate_nesting_2a}
\alias{validate_nesting_2a}
\title{Validate the Chapter 1 nesting / no-false-positive behaviour}
\usage{
validate_nesting_2a(
  n = 120,
  mu_g = c(0, 3, 2.3, -4, -1),
  mu_a = c(0.2, 3.1, 2.2, -3.8, -1.1),
  sigma_g = diag(c(0.09, 0.16, 0.09, 0.16, 0.09)),
  sigma_a = diag(c(0.09, 0.16, 0.09, 0.16, 0.09)),
  noise_sd = 0.15,
  seed = 1,
  ...
)
}
\arguments{
\item{n}{\link{integer} number of subjects. Default \code{120}.}

\item{mu_g, mu_a, sigma_g, sigma_a}{Model 2a truth (cross-block forced to zero).}

\item{noise_sd}{Residual SD on the log scale. Default \code{0.15}.}

\item{seed}{RNG seed. Default \code{1}.}

\item{...}{MCMC controls forwarded to \code{\link[=run_mod_2a]{run_mod_2a()}}.}
}
\value{
A \link{data.frame} with columns \code{param}, \code{cov_med}, \code{cov_lo}, \code{cov_hi},
and \code{covers_zero} (\link{logical}); all rows should have \code{covers_zero = TRUE}.
}
\description{
Complementary check to \code{\link[=validate_recovery_2a]{validate_recovery_2a()}}. It simulates \strong{independent}
two-biomarker data (all cross-biomarker covariances \code{c_vec = 0}, i.e. the
Chapter 1 truth) and confirms that Model 2a does \strong{not} invent
cross-biomarker correlation: every posterior \code{c_p} credible interval should
cover zero. This is the empirical counterpart of the algebraic fact that
\code{lambda = 0} reduces Model 2a to Chapter 1.

The packaged typhoid simulator \code{\link[=sim_case_data]{sim_case_data()}} also produces independent
biomarkers, so fitting Model 2a to its output is an equivalent real-data-style
null check.
}
