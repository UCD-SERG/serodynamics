% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_priors_multi_b.R
\name{prep_priors_multi_b}
\alias{prep_priors_multi_b}
\title{Priors for the Kronecker (multi-biomarker) model}
\usage{
prep_priors_multi_b(
  n_blocks,
  omega_p_scale = rep(0.1, 5),
  nu_p = 6,
  omega_b_scale = rep(1, n_blocks),
  nu_b = n_blocks + 1
)
}
\arguments{
\item{n_blocks}{Integer scalar (B): number of biomarkers.}

\item{omega_p_scale}{Numeric length-5 vector for the diagonal of Omega_P
(parameter scale).}

\item{nu_p}{Numeric scalar: degrees of freedom for
\code{T_P ~ Wishart(Omega_P, nu_p)}.}

\item{omega_b_scale}{Numeric length-\code{n_blocks} vector for the diagonal of
Omega_B (biomarker scale).}

\item{nu_b}{Numeric scalar: degrees of freedom for
\code{T_B ~ Wishart(Omega_B, nu_b)}.}
}
\value{
A list with elements \code{OmegaP}, \code{nuP}, \code{OmegaB}, \code{nuB}.
}
\description{
\code{prep_priors_multi_b()} builds Wishart hyperparameters for the
within-biomarker precision matrix \code{T_P} and the across-biomarker precision
matrix \code{T_B} used in the Kronecker prior \code{T = T_B \%x\% T_P}.
}
\examples{
# Basic usage: 3 biomarkers, weakly-informative defaults
pri <- prep_priors_multi_b(n_blocks = 3)
str(pri)

# Custom scales (and degrees of freedom)
pri_custom <- prep_priors_multi_b(
  n_blocks      = 4,
  omega_p_scale = c(0.2, 0.2, 0.3, 0.3, 0.4),
  nu_p          = 7,
  omega_b_scale = rep(1.5, 4),
  nu_b          = 6
)
str(pri_custom)
}
\author{
Kwan Ho Lee
}
