% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_case_data_kl.R
\name{sim_case_data_kl}
\alias{sim_case_data_kl}
\title{Simulate longitudinal case follow-up data from a homogeneous population}
\usage{
sim_case_data_kl(
  n,
  curve_params,
  antigen_isos = get_biomarker_levels(curve_params),
  max_n_obs = 10,
  dist_n_obs = tibble::tibble(n_obs = 1:max_n_obs, prob = 1/max_n_obs),
  followup_interval = 7,
  followup_variance = 1,
  noise_sd = 0,
  spacing = c("uniform", "log"),
  t_min = 3,
  t_max = 400
)
}
\arguments{
\item{n}{\link{integer} number of cases to simulate}

\item{curve_params}{a \code{curve_params}
object from \link[serocalculator:as_curve_params]{serocalculator::as_curve_params}, assumed to be unstratified}

\item{antigen_isos}{\link{character} \link{vector}: which antigen isotypes to simulate}

\item{max_n_obs}{maximum number of observations}

\item{dist_n_obs}{distribution of number of observations (\link[tibble:tbl_df]{tibble::tbl_df})}

\item{followup_interval}{\link{integer} mean days between visits (used when
\code{spacing = "uniform"})}

\item{followup_variance}{\link{integer} jitter (+/- days) between visits (used
when \code{spacing = "uniform"})}

\item{noise_sd}{\link{numeric} measurement-error SD \strong{on the log scale}. Length 1
(recycled) or one per element of \code{antigen_isos}; may be named by antigen.
\code{0} (default) means no measurement error.}

\item{spacing}{\link{character} visit-time scheme passed to \link{sim_obs_times}:
\code{"uniform"} (default, original equally-spaced visits) or \code{"log"} (new;
log-spaced visits -- dense early, sparse late -- to capture both the peak
and the decay tail). See \link{sim_obs_times}.}

\item{t_min}{\link{numeric} first non-zero visit day for the log grid
(default 3; used only when \code{spacing = "log"})}

\item{t_max}{\link{numeric} last visit day for the log grid, reached by a case
with \code{max_n_obs} visits (default 400; used only when \code{spacing = "log"})}
}
\value{
a \code{case_data} object
}
\description{
Simulate longitudinal case follow-up data from a homogeneous population
}
\section{What changed in this version (and why)}{


\strong{ADDED -- \code{noise_sd}: measurement error on the log scale.}
Both models include an observation-error term:
\code{model.jags} line 54 (\code{logy ~ dnorm(mu.logy, prec.logy)}; the line-17 comment
calls \code{mu.logy} the value \emph{"before Gaussian noise is added"}) and
\code{model_ch2.stan} line 110
(\code{logy ~ normal(two_phase_logk(...), inv_sqrt(prec_logy[k]))}).
This function previously returned \code{value = ab(...)} with no noise, so every
simulated point sat exactly on the curve and the residual was zero -- the
model was asked to estimate a parameter the data-generating process did not
have. Ezra: \emph{"All the parameters that are in the chapter one model need to go
into \code{make_corr_curve_params}"} -- \code{prec.logy} is one of them.

The noise is \strong{multiplicative on the natural scale} (equivalently additive on
the log scale), matching the models:
\code{value = ab(...) * exp(N(0, noise_sd))}.
Adding it on the natural scale instead would (a) invert the error structure --
a log-scale SD of 0.29 is a ~29\% error everywhere, whereas adding 0.29 to the
natural value is ~29\% at baseline and ~0.2\% at peak -- and (b) produce
negative values, which become \code{NaN} at \code{prep_ch2_standata()} line 49
(\code{logy <- log(value)}).

\code{noise_sd} is per antigen-isotype because the models have a separate
\code{prec_logy[k]} for each. The posterior means from the \code{nepal_sees} fit are
\verb{prec_logy = (11.969, 10.720)}, i.e. \code{noise_sd = c(0.2890, 0.3054)} for
\verb{(HlyE_IgG, HlyE_IgA)}.

\strong{Backward compatible:} the default \code{noise_sd = 0} skips the noise step
entirely (it does not draw from the RNG), so existing seeded runs reproduce
bit-for-bit and the \code{rho = 0} nesting check is unaffected.
}

\examples{
set.seed(1)
# original uniform scheme:
serocalculator::typhoid_curves_nostrat_100 |>
  sim_case_data(n = 100)

# log-spaced visits with realistic measurement error:
serocalculator::typhoid_curves_nostrat_100 |>
  sim_case_data(n = 100, max_n_obs = 15, spacing = "log",
                noise_sd = c(0.2890, 0.3054))
}
