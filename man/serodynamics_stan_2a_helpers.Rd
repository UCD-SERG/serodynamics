% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serodynamics_stan_2a_helpers.R
\name{serodynamics_stan_2a_helpers}
\alias{serodynamics_stan_2a_helpers}
\alias{summarise_pop_2a}
\alias{log_two_phase_r}
\alias{predict_newperson_2a}
\title{Chapter 2 Stan helpers}
\usage{
summarise_pop_2a(fit, antigens = NULL)

log_two_phase_r(t, log_y0, log_y1, t1, alpha, shape)

predict_newperson_2a(
  mu_draws,
  Sigma_draws,
  predict_antigen = 2L,
  given = NULL,
  n_params = 5L
)
}
\arguments{
\item{fit}{a CmdStanR fit from \code{\link[=run_serodynamics_stan_2a]{run_serodynamics_stan_2a()}}.}

\item{antigens}{optional \link{character} vector of antigen labels (defaults to the
\code{"antigens"} attribute on \code{fit}, else \verb{A1, A2, ...}).}

\item{t}{numeric vector of times.}

\item{log_y0, log_y1}{log baseline and log peak.}

\item{t1}{time to peak.}

\item{alpha}{decay rate.}

\item{shape}{decay shape \code{r} (\verb{> 1}).}

\item{mu_draws}{matrix \verb{[ndraws, n_total]} of population-mean draws (stacked
as antigen-major: antigen 1's 5 params, then antigen 2's 5).}

\item{Sigma_draws}{array \verb{[ndraws, n_total, n_total]} of joint covariance
draws (use a block-diagonal Sigma for the independent baseline).}

\item{predict_antigen}{index (1 or 2) of the biomarker to predict.}

\item{given}{optional named or positional length-5 vector of observed log
parameters for the OTHER biomarker (NULL = marginal prediction).}

\item{n_params}{number of curve parameters (default 5).}
}
\value{
a \link{data.frame} with columns \code{antigen}, \code{param}, \code{mean}, \code{q5}, \code{q95},
\code{sd}, \code{rhat}, \code{ess_bulk}.

numeric vector \verb{log y(t)}.

matrix \verb{[ndraws, n_params]} of predicted log parameters for the new
subject's \code{predict_antigen}.
}
\description{
Small post-processing helpers for the Chapter 2 fits: a tidy population-mean
summary, the two-phase antibody curve on the log scale (matching the Stan
\code{log_two_phase} and the package's \code{\link[serocalculator:ab]{ab()}}), and a Gaussian conditional
new-person prediction used to compare Model 2a against the independent
baseline.
}
\section{Functions}{
\itemize{
\item \code{summarise_pop_2a()}: Tidy posterior summary of the
population means \code{mu_par} (one row per antigen x parameter).

\item \code{log_two_phase_r()}: Two-phase antibody curve on the log
scale (identical to the Stan \code{log_two_phase}).

\item \code{predict_newperson_2a()}: Conditional new-person prediction.
Given posterior draws of the joint population mean and covariance, predict
the log curve parameters of one biomarker (\code{predict_antigen}) for a new
subject, optionally CONDITIONING on observed log parameters of the other
biomarker (\code{given}). With cross-biomarker covariance (Model 2a) the
conditioning tightens the prediction; with a block-diagonal covariance
(independent) it does not. Returns posterior-predictive draws of that
biomarker's 5 log parameters, which you can push through
\code{\link[=log_two_phase_r]{log_two_phase_r()}} to get curve credible intervals.

}}
\author{
Kwan Ho Lee
}
